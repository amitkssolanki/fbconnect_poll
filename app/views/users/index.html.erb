<h1>My Friends</h1>
<ul>
	<% @friends.each do |friend| %>
	<li>
		<fb:profile-pic uid='<%= friend.id %>' facebook-logo="true" size="thumb" ></fb:profile-pic><br />
		<%= fb_name(friend.id) %>
	</li>
	<% end %>
</ul>